services:
  - docker

script:
  - make lint
  - make build

deploy:
  provider: pages
  skip_cleanuo: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: main
  local_dir: ./dist
